import styled from 'styled-components';

const Tel = styled.td`
  font-size: 25px;
  max-width: 400px;
  text-overflow: ellipsis;
  white-space: nowrap;
  word-wrap: normal;
  overflow: hidden;
`;

export default Tel;
